# triplex-storybook--docs-components-selectextendedfield--docs

Source: https://storybook.triplex-dev.ru/main/?path=/docs/components-selectextendedfield--docs

---

# SelectExtendedField

SelectExtendedField - это расширенный компонент выбора, который позволяет создавать кастомные селекты с гибкой настройкой target и dropdown элементов.

## Основные возможности:

* Кастомный рендер target элемента
* Кастомный рендер dropdown списка
* Поддержка всех состояний (disabled, error, loading)
* Навигация с клавиатуры
* Автоматическое позиционирование dropdown
* Поддержка мобильных устройств

## Использование:

Компонент принимает две рендер-функции:

* `renderTarget` \- для рендера поля выбора
* `children` \- для рендера выпадающего списка

Каждая функция получает необходимые пропсы для управления состоянием.

| Name            | Description                             | Default                  | Control                                             |
| --------------- | --------------------------------------- | ------------------------ | --------------------------------------------------- |
| fieldLabel      | Текст лейбла поляstring                 | Выберите опцию           | Выберите опцию                                      |
| placeholder     | Текст плейсхолдераstring                | Выберите опцию из списка | Выберите опцию из списка                            |
| loading         | Состояние загрузкиboolean               | false                    | FalseTrue                                           |
| status          | Отключенное состояниеEFormFieldStatus   | EFormFieldStatus.DEFAULT | Choose option...defaultdisablederrorwarning         |
| descriptionText | Текст описания поляstring               | \-                       | Описание поля                                       |
| errorText       | Текст ошибкиstring                      | \-                       | Текст ошибки                                        |
| selectedValue   | Предварительно выбранное значениеstring | ""                       | Choose option...option1option2option3option4option5 |

Выберите опцию

Выберите опцию из списка

Описание поля

Show code

## Stories

### Playground

Интерактивная демонстрация SelectExtendedField с расширенными controls. Позволяет настраивать все основные свойства компонента, включая текст лейбла, плейсхолдер, состояния (отключенное, ошибка, загрузка) и предварительно выбранное значение. Также включает отладочную информацию для демонстрации состояния компонента.

Выберите опцию

Выберите опцию из списка

Описание поля

Show code

### Basic

Базовый пример использования SelectExtendedField с простым списком опций.

Выберите опцию

Выберите опцию из списка

Show code

### States

Демонстрация различных состояний SelectExtendedField: обычное, загрузка, ошибка и отключенное состояние.

### Обычное состояние

Выберите опцию

Вторая опция

### Состояние загрузки

Загрузка опций

Вторая опция

### Состояние ошибки

Выберите опцию

Вторая опция

Обязательное поле

### Состояние предупреждения

Выберите опцию

Вторая опция

Обязательное поле

### Отключенное состояние

Отключенное поле

Вторая опция

Поле временно недоступно

Show code
