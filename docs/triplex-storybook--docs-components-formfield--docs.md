# triplex-storybook--docs-components-formfield--docs

Source: https://storybook.triplex-dev.ru/main/?path=/docs/components-formfield--docs

---

# FormField

Компонент FormField представляет собой универсальное поле ввода с поддержкой различных элементов:

## Основные возможности

* **FormFieldInput**  
   * текстовое поле ввода
* **FormFieldTextarea**  
   * многострочное поле ввода
* **FormFieldMaskedInput**  
   * поле ввода с маской (телефоны, карты, даты и др.)
* **FormFieldLabel**  
   * плавающий лейбл
* **FormFieldClear**  
   * кнопка очистки
* **FormFieldPrefix/Postfix**  
   * элементы слева/справа от поля
* **FormFieldDescription**  
   * описание под полем
* **Размеры**  
   * SM (маленький), MD (средний), LG (большой - по умолчанию)

| Name   | Description                                    | Default                  | Control |
| ------ | ---------------------------------------------- | ------------------------ | ------- |
| status | Визуальное состояние.EFormFieldStatusundefined | EFormFieldStatus.DEFAULT | \-      |
| size   | Размер.EComponentSizeundefined                 | EComponentSize.LG        | \-      |

Название поля

Описание поля

0/201

Show code

## Stories

### Playground

Интерактивная демонстрация FormField с расширенными controls. Позволяет настраивать все основные свойства компонента, включая тип поля, текст лейбла, плейсхолдер, отображение кнопки очистки и описания. Также включает отладочную информацию для демонстрации состояния компонента.

Название поля

Описание поля

0/201

Show code

### Default

Название поля

Show code

### Basic

Базовые примеры использования FormField с различными типами полей ввода.

Имя пользователя

Show code

### With Prefix And Postfix

FormField с префиксами и постфиксами. Префиксы отображаются слева от поля, постфиксы - справа.

Название поля

Show code

### With Clear Button

FormField с префиксами и постфиксами. Префиксы отображаются слева от поля, постфиксы - справа.

Название поля

Show code

### With Counter

FormField со счетчиком символов. Счетчик показывает текущее количество символов и максимально допустимое.

Название поля

Описание поля

0/201

Show code

### States

Различные состояния FormField: обычное, с ошибкой, отключенное.

Название поля

Описание поля

Название поля

Текст ошибки

Название поля

Текст предупреждения

Название поля

Описание поля

Show code

### Textarea

FormField с многострочным полем ввода (textarea). Счетчик показывает текущее количество символов и максимально допустимое.

Название поля

Описание поля

0/201

Show code

### Sizes

Демонстрация различных размеров FormField: SM (маленький), MD (средний - по умолчанию), LG (большой). Каждый размер имеет свои отступы и высоту для разных случаев использования.

### Размер SM (маленький)

Маленькое поле

### Размер MD (средний) - по умолчанию

Среднее поле

### Размер LG (большой)

Большое поле

Show code

### Masked Input

FormField с маскированным вводом. Поддерживает различные предустановленные маски для телефонных номеров, номеров карт, дат и других форматов данных.

Номер телефона

Описание поля

Номер карты

Описание поля

Show code
